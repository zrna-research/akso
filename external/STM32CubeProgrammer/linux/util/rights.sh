#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
chmod +x $DIR/../bin/STM32CubeProgrammerLauncher
chmod +x $DIR/../bin/STM32CubeProgrammer
chmod +x $DIR/../bin/STM32_Programmer_CLI
chmod +x $DIR/../bin/STM32_Programmer.sh
chmod +x $DIR/../util/openJFXScript.csh 